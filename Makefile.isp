ISP_PREFIX ?= $(HOME)/.local/isp/

install-%: %
	mkdir -p $(ISP_PREFIX)/bsp/$^/ap/src
	cp -r $^/bsp/* $(ISP_PREFIX)/bsp/$^/ap/src

install: install-hifive install-ssith-p1 install-ssith-p2 install-iveia

clean: